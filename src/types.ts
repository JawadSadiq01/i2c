export type HeroCardData = {
  image: string;
  name: string;
  detail: string;
}

export type HeroCardProps = {
  data: HeroCardData[];
}
export type MoreDetailData = {
  image: string;
  name: string;
  detail: string;
}

export type MoreDetailProps = {
  data: HeroCardData[];
}